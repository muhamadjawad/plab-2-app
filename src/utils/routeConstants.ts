const HOME_ROUTE: string = 'home' as const
const QUESTION_READING_ROUTE: string = 'question_reading'  as const
const ENCOUNTER_ROUTE: string = 'encounter'  as const

export {
    HOME_ROUTE,
    QUESTION_READING_ROUTE,
    ENCOUNTER_ROUTE,
}